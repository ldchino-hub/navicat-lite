NAVICAT_PHP_PORT=8788

# Generate: openssl rand -hex 32
META_ENC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Generate: openssl rand -hex 16
JWT_SECRET=change-me-jwt-secret-min-16-chars

ADMIN_EMAIL=ldchino@gmail.com
ADMIN_PASSWORD=
SEED_ADMIN=true

# Optional: remote MySQL via HTTP bridge (shared hosting / blocked ports)
MYSQL_BRIDGE_URL=
MYSQL_BRIDGE_KEY=
MYSQL_BRIDGE_HOSTS=
